<UserControl x:Class="PortLog.Views.VoyageListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <!-- Shadow Effect -->
        <DropShadowEffect x:Key="CardShadow" 
                          BlurRadius="20" 
                          ShadowDepth="3" 
                          Opacity="0.15" 
                          Color="#000000"/>

        <!-- Button Style -->
        <Style x:Key="SearchButton" TargetType="Button">
            <Setter Property="Background" Value="#0081A7"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="18 8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#006d8f"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- DatePicker Style -->
        <Style x:Key="ModernDatePicker" TargetType="DatePicker">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Padding" Value="10 8"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- DataGrid Style -->
        <Style x:Key="ModernDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F8F9FA"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
        </Style>

        <!-- DataGrid Header Style -->
        <Style x:Key="ModernColumnHeader" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F8F9FA"/>
            <Setter Property="Foreground" Value="#666666"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="15 12"/>
            <Setter Property="BorderThickness" Value="0 0 0 2"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
        </Style>

        <!-- DataGrid Cell Style -->
        <Style x:Key="ModernCell" TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15 10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E3F2FD"/>
                    <Setter Property="Foreground" Value="#0081A7"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- DataGrid Row Style -->
        <Style x:Key="ModernRow" TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F5F5F5"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E3F2FD"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="40" Background="#F8F9FA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ====== HEADER ====== -->
        <StackPanel Orientation="Horizontal" Margin="0 0 0 30">
            <TextBlock Text="🚢" FontSize="36" VerticalAlignment="Center" Margin="0 0 15 0"/>
            <TextBlock Text="Voyage Log" 
                       FontWeight="Bold"
                       FontFamily="Plus Jakarta Sans"
                       FontSize="32"
                       VerticalAlignment="Center"
                       Foreground="#1A1A1A"/>
        </StackPanel>

        <!-- ====== FILTER BAR ====== -->
        <Border Grid.Row="1"
                Background="White" 
                CornerRadius="12" 
                Padding="25"
                Effect="{StaticResource CardShadow}"
                Margin="0 0 0 25">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <!-- START DATE -->
                <StackPanel Margin="0 0 20 0">
                    <TextBlock Text="START DATE"
                               FontSize="11"
                               FontWeight="SemiBold"
                               Foreground="#666666"
                               Margin="0 0 0 8"/>
                    <DatePicker SelectedDate="{Binding StartDate}"
                                Width="160"
                                Height="36"
                                Style="{StaticResource ModernDatePicker}"/>
                </StackPanel>

                <!-- ARROW -->
                <TextBlock Text="→" 
                           FontSize="24" 
                           VerticalAlignment="Bottom" 
                           Margin="0 0 20 8"
                           Foreground="#CCCCCC"/>

                <!-- END DATE -->
                <StackPanel Margin="0 0 20 0">
                    <TextBlock Text="END DATE"
                               FontSize="11"
                               FontWeight="SemiBold"
                               Foreground="#666666"
                               Margin="0 0 0 8"/>
                    <DatePicker SelectedDate="{Binding EndDate}"
                                Width="160"
                                Height="36"
                                Style="{StaticResource ModernDatePicker}"/>
                </StackPanel>

                <!-- SEARCH BUTTON -->
                <Button Content="Search"
                        Width="100"
                        Height="36"
                        VerticalAlignment="Bottom"
                        Command="{Binding SearchCommand}"
                        Style="{StaticResource SearchButton}"/>
            </StackPanel>
        </Border>

        <!-- ====== DATA TABLE ====== -->
        <Border Grid.Row="2"
                Background="White"
                CornerRadius="12"
                Effect="{StaticResource CardShadow}">
            <DataGrid ItemsSource="{Binding Voyages}"
                      Style="{StaticResource ModernDataGrid}"
                      ColumnHeaderStyle="{StaticResource ModernColumnHeader}"
                      CellStyle="{StaticResource ModernCell}"
                      RowStyle="{StaticResource ModernRow}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      FontSize="14">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="DEPARTURE TIME"
                                        Binding="{Binding DepartureTime, StringFormat=\{0:dd MMM yyyy HH:mm\}}"
                                        Width="*"
                                        MinWidth="170"/>
                    <DataGridTextColumn Header="ARRIVAL TIME"
                                        Binding="{Binding ArrivalTime, StringFormat=\{0:dd MMM yyyy HH:mm\}}"
                                        Width="*"
                                        MinWidth="170"/>
                    <DataGridTextColumn Header="DEPARTURE PORT"
                                        Binding="{Binding DeparturePort}" 
                                        Width="*"
                                        MinWidth="150"/>
                    <DataGridTextColumn Header="ARRIVAL PORT"
                                        Binding="{Binding ArrivalPort}" 
                                        Width="*"
                                        MinWidth="150"/>
                    <DataGridTextColumn Header="SHIP NAME"
                                        Binding="{Binding ShipName}" 
                                        Width="*"
                                        MinWidth="150"/>
                    <DataGridTextColumn Header="REVENUE (IDR)"
                                        Binding="{Binding Revenue, StringFormat=N0}" 
                                        Width="*"
                                        MinWidth="150">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#00B894"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
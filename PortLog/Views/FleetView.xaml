<UserControl x:Class="PortLog.Views.FleetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">


    <Grid Margin="20">
        <DataGrid ItemsSource="{Binding Fleets}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  RowHeight="40"
                  HeadersVisibility="Column"
                  GridLinesVisibility="None"
                  FontSize="14">

            <DataGrid.Columns>

                <DataGridTextColumn Header="Name"
                                    Binding="{Binding Name}" Width="*"/>

                <DataGridTextColumn Header="Type"
                                    Binding="{Binding Type}" Width="150"/>

                <DataGridTextColumn Header="Captain"
                                    Binding="{Binding CaptainName}" Width="200"/>

                <DataGridTextColumn Header="Last Voyage"
                                    Binding="{Binding LastVoyage}" Width="250"/>

                <DataGridTextColumn Header="Status"
                                    Binding="{Binding Status}" Width="100"/>

                <DataGridTextColumn Header="Capacity"
                                    Binding="{Binding Capacity}" Width="150"/>

                <DataGridTemplateColumn Header="Detail" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Detail"
                                    Padding="5"
                                    Command="{Binding DataContext.DetailCommand, 
                                              RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding ShipId}"
                                    Background="#0081A7"
                                    Foreground="White"
                                    BorderThickness="0"
                                    Width="80"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</UserControl>

<UserControl x:Class="PortLog.Views.Register3View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1920">

    <UserControl.Resources>
        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1D4ED8"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#94A3B8"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#2563EB"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#EFF6FF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Input TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#2563EB"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F8FAFC">
        <!-- Main Content -->
        <Grid MaxWidth="500" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="40">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,32" Width="500">
                <TextBlock Text="Buat Profil Perusahaan" 
                          FontSize="32" 
                          FontWeight="Bold" 
                          Foreground="#0F172A"
                          HorizontalAlignment="Center"/>
                <TextBlock Text="Langkah 3 dari 3: Detail Perusahaan" 
                          FontSize="14" 
                          Foreground="#64748B"
                          HorizontalAlignment="Center"
                          Margin="0,8,0,0"/>
            </StackPanel>

            <!-- Form Card -->
            <Border Grid.Row="1" 
                    Background="White" 
                    CornerRadius="12" 
                    Padding="40, 20"
                    Style="{DynamicResource CardShadow}">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" 
                        Opacity="0.08" 
                        ShadowDepth="2" 
                        BlurRadius="16"/>   
                </Border.Effect>

                <StackPanel>
                    <!-- Info Banner -->
                    <Border Background="#DBEAFE" 
                           BorderBrush="#3B82F6" 
                           BorderThickness="1"
                           CornerRadius="8"
                           Padding="8"
                           Margin="0,0,0,4">
                        <StackPanel>
                            <TextBlock Text="🎉 Selamat! Anda akan menjadi Manager Perusahaan" 
                                      FontSize="14"
                                      FontWeight="SemiBold"
                                      Foreground="#1E40AF"/>
                            <TextBlock Text="Kode join akan dibuat otomatis untuk perusahaan Anda" 
                                      FontSize="12"
                                      Foreground="#1E40AF"
                                      Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Company Name -->
                    <TextBlock Text="Nama Perusahaan *" 
                              FontSize="14" 
                              FontWeight="SemiBold" 
                              Foreground="#334155"
                              Margin="0,0,0,8"/>
                    <TextBox Style="{StaticResource ModernTextBox}"
                            Text="{Binding CompanyName, UpdateSourceTrigger=PropertyChanged}"
                            Margin="0,0,0,20"/>

                    <!-- Address -->
                    <TextBlock Text="Alamat" 
                              FontSize="14" 
                              FontWeight="SemiBold" 
                              Foreground="#334155"
                              Margin="0,0,0,8"/>
                    <TextBox Style="{StaticResource ModernTextBox}"
                            Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}"
                            AcceptsReturn="True"
                            TextWrapping="Wrap"
                            MinHeight="80"
                            VerticalScrollBarVisibility="Auto"
                            Margin="0,0,0,20"/>

                    <!-- Province -->
                    <TextBlock Text="Provinsi" 
                              FontSize="14" 
                              FontWeight="SemiBold" 
                              Foreground="#334155"
                              Margin="0,0,0,8"/>
                    <TextBox Style="{StaticResource ModernTextBox}"
                            Text="{Binding Provinsi, UpdateSourceTrigger=PropertyChanged}"
                            Margin="0,0,0,20"/>

                    <!-- Error Message -->
                    <Border Background="#FEE2E2" 
                           BorderBrush="#EF4444" 
                           BorderThickness="1"
                           CornerRadius="8"
                           Padding="12"
                           Margin="0,0,0,20"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}">
                        <TextBlock Text="{Binding ErrorMessage}" 
                                  Foreground="#DC2626" 
                                  FontSize="13"
                                  TextWrapping="Wrap"/>
                    </Border>

                    <!-- Loading Indicator -->
                    <Border Background="#EFF6FF" 
                           BorderBrush="#3B82F6" 
                           BorderThickness="1"
                           CornerRadius="8"
                           Padding="5"
                           Margin="0,0,0,20"
                           Visibility="{Binding IsCreating, Converter={StaticResource BoolToVisibilityConverter}}">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <TextBlock Text="⏳" FontSize="16" Margin="0,0,8,0"/>
                            <TextBlock Text="Membuat perusahaan..." 
                                      FontSize="14"
                                      FontWeight="Medium"
                                      Foreground="#1E40AF"/>
                        </StackPanel>
                    </Border>

                    <!-- Buttons -->
                    <Grid Margin="0,12,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="0"
                               Content="Kembali"
                               Style="{StaticResource SecondaryButton}"
                               Command="{Binding BackCommand}"
                               IsEnabled="{Binding IsCreating, Converter={StaticResource InverseBoolConverter}}"/>

                        <Button Grid.Column="2"
                               Content="Buat &amp; Selesai"
                               Style="{StaticResource ModernButton}"
                               Command="{Binding CreateAndJoinCommand}"/>
                    </Grid>

                    <!-- Help Text -->
                    <TextBlock FontSize="12"
                              Foreground="#64748B"
                              HorizontalAlignment="Center"
                              TextAlignment="Center"
                              Margin="0,20,0,0"
                              TextWrapping="Wrap">
                        <Run Text="Dengan membuat perusahaan, Anda akan menjadi manager."/>
                        <LineBreak/>
                        <Run Text="Anda dapat mengundang anggota lain dengan kode join di profil."/>
                    </TextBlock>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
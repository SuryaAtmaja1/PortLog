<UserControl x:Class="PortLog.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.InputBindings>
        <KeyBinding Key="Enter"
                Command="{Binding LoginCommand}"
                CommandParameter="{Binding ElementName=PasswordBox}"/>
    </UserControl.InputBindings>

    <Grid Background="#F5F9F9">
        <Border MaxWidth="440" MaxHeight="450" Background="#F5F9F9" 
                CornerRadius="20" Padding="40" VerticalAlignment="Center"
                HorizontalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="3" Opacity="0.3"/>
            </Border.Effect>

            <StackPanel Width="340">
                <TextBlock Text="Sign In" FontSize="32" FontWeight="Medium" FontStyle="Italic"
                          HorizontalAlignment="Center" Margin="0,0,0,30"/>
                <TextBox Style="{StaticResource TextBox_1}" Tag="Email" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>
                <PasswordBox x:Name="PasswordBox" Style="{StaticResource PasswordBox_1}" Tag="Password" PasswordChanged="PasswordBox_OnPasswordChanged" RenderTransformOrigin="0.502,1.686"/>

                <TextBlock Text="{Binding ErrorMessage}" 
                          Foreground="Red" 
                          Margin="0,10"
                          Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

                <Button Style="{StaticResource Button_2}"
                       Content="Sign In" 
                       Command="{Binding LoginCommand}"
                       CommandParameter="{Binding ElementName=PasswordBox}"
                       Margin="0,0,0,10" Width="90" />

                <Button Content="Register" 
                       Command="{Binding NavigateToRegisterCommand}"
                       Background="#6C757D"
                       Margin="0,5"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
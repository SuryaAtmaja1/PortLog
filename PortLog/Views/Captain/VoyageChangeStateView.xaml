<UserControl x:Class="PortLog.Views.VoyageChangeStateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <!-- Shadow Effects -->
        <DropShadowEffect x:Key="CardShadow"
                          BlurRadius="20"
                          ShadowDepth="3"
                          Opacity="0.15"
                          Color="#000000"/>

        <!-- Primary Button Style -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#0081A7"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="14 7"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#006d8f"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#7B1FA2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="14 7"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#6A1B9A"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- DataGrid Style -->
        <Style x:Key="ModernDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F8F9FA"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
        </Style>

        <!-- DataGrid Header Style -->
        <Style x:Key="ModernColumnHeader" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F8F9FA"/>
            <Setter Property="Foreground" Value="#666666"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="15 12"/>
            <Setter Property="BorderThickness" Value="0 0 0 2"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
        </Style>

        <!-- DataGrid Cell Style -->
        <Style x:Key="ModernCell" TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15 10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E3F2FD"/>
                    <Setter Property="Foreground" Value="#0081A7"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- DataGrid Row Style -->
        <Style x:Key="ModernRow" TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F5F5F5"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E3F2FD"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F8F9FA">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="40,30" MaxWidth="1400">

                <!-- ====== HEADER ====== -->
                <StackPanel Margin="0 0 0 30">
                    <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
                        <TextBlock Text="📡" FontSize="36" VerticalAlignment="Center" Margin="0 0 15 0"/>
                        <TextBlock Text="Voyage State Management" 
                                   FontWeight="Bold"
                                   FontFamily="Plus Jakarta Sans"
                                   FontSize="32"
                                   VerticalAlignment="Center"
                                   Foreground="#1A1A1A"/>
                    </StackPanel>
                    <TextBlock Text="Monitor and change operational status of your fleet" 
                               FontSize="14"
                               Foreground="#666666"
                               Margin="51 0 0 0"/>
                </StackPanel>

                <!-- ====== SAILING SHIPS SECTION ====== -->
                <Border Background="White" 
                        CornerRadius="16" 
                        Padding="30" 
                        Margin="0,0,0,25"
                        Effect="{StaticResource CardShadow}">
                    <StackPanel>
                        <!-- Section Header -->
                        <Border Background="#E8F5E9" 
                                CornerRadius="12" 
                                Padding="20,15" 
                                Margin="0,0,0,25">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="45" Height="45" 
                                        Background="#66BB6A" 
                                        CornerRadius="10"
                                        Padding="8"
                                        Margin="0 0 15 0">
                                    <TextBlock Text="⛴️" 
                                               FontSize="24"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                </Border>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="Sailing Ships"
                                               FontSize="20" 
                                               FontWeight="Bold" 
                                               Foreground="#2E7D32"/>
                                    <TextBlock Text="Currently active vessels at sea"
                                               FontSize="12"
                                               Foreground="#666666"
                                               Margin="0 2 0 0"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- DataGrid -->
                        <DataGrid ItemsSource="{Binding SailingShips}"
                                  Style="{StaticResource ModernDataGrid}"
                                  ColumnHeaderStyle="{StaticResource ModernColumnHeader}"
                                  CellStyle="{StaticResource ModernCell}"
                                  RowStyle="{StaticResource ModernRow}"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True"
                                  RowHeight="55"
                                  FontSize="14">

                            <DataGrid.Columns>
                                <!-- Ship Name with Icon -->
                                <DataGridTemplateColumn Header="SHIP NAME" Width="*" MinWidth="150">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="🚢" FontSize="16" Margin="0 0 8 0" VerticalAlignment="Center"/>
                                                <TextBlock Text="{Binding Name}" 
                                                           FontWeight="SemiBold" 
                                                           VerticalAlignment="Center"
                                                           Foreground="#1A1A1A"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <DataGridTextColumn Header="TYPE" Binding="{Binding Type}" Width="120"/>

                                <!-- Captain with Icon -->
                                <DataGridTemplateColumn Header="CAPTAIN" Width="*" MinWidth="150">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="👤" FontSize="14" Margin="0 0 6 0" VerticalAlignment="Center"/>
                                                <TextBlock Text="{Binding CaptainName}" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <DataGridTextColumn Header="LAST VOYAGE" Binding="{Binding LastVoyage}" Width="*" MinWidth="180"/>

                                <!-- Capacity with Icon -->
                                <DataGridTemplateColumn Header="CAPACITY" Width="120">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="📦" FontSize="14" Margin="0 0 6 0" VerticalAlignment="Center"/>
                                                <TextBlock Text="{Binding Capacity}" 
                                                           VerticalAlignment="Center"
                                                           FontWeight="SemiBold"
                                                           Foreground="#0081A7"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <!-- Actions -->
                                <DataGridTemplateColumn Header="ACTIONS" Width="260">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                                <Button Content="Detail"
                                                        Style="{StaticResource PrimaryButton}"
                                                        Width="80"
                                                        Margin="0,0,8,0"
                                                        Command="{Binding DataContext.DetailCommand,
                                                                  RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding ShipId}"/>

                                                <Button Content="Change State"
                                                        Style="{StaticResource SecondaryButton}"
                                                        Width="110"
                                                        Command="{Binding DataContext.ChangeStateCommand,
                                                                  RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding ShipId}"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

                <!-- ====== NOT SAILING SHIPS SECTION ====== -->
                <Border Background="White" 
                        CornerRadius="16" 
                        Padding="30"
                        Effect="{StaticResource CardShadow}">
                    <StackPanel>
                        <!-- Section Header -->
                        <Border Background="#FFF3E0" 
                                CornerRadius="12" 
                                Padding="20,15" 
                                Margin="0,0,0,25">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="45" Height="45" 
                                        Background="#FFA726" 
                                        CornerRadius="10"
                                        Padding="8"
                                        Margin="0 0 15 0">
                                    <TextBlock Text="⚓" 
                                               FontSize="24"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                </Border>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="Docked Ships"
                                               FontSize="20" 
                                               FontWeight="Bold" 
                                               Foreground="#E65100"/>
                                    <TextBlock Text="Vessels currently in port or maintenance"
                                               FontSize="12"
                                               Foreground="#666666"
                                               Margin="0 2 0 0"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- DataGrid -->
                        <DataGrid ItemsSource="{Binding NotSailingShips}"
                                  Style="{StaticResource ModernDataGrid}"
                                  ColumnHeaderStyle="{StaticResource ModernColumnHeader}"
                                  CellStyle="{StaticResource ModernCell}"
                                  RowStyle="{StaticResource ModernRow}"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True"
                                  RowHeight="55"
                                  FontSize="14">

                            <DataGrid.Columns>
                                <!-- Ship Name with Icon -->
                                <DataGridTemplateColumn Header="SHIP NAME" Width="*" MinWidth="140">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="🚢" FontSize="16" Margin="0 0 8 0" VerticalAlignment="Center"/>
                                                <TextBlock Text="{Binding Name}" 
                                                           FontWeight="SemiBold" 
                                                           VerticalAlignment="Center"
                                                           Foreground="#1A1A1A"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <DataGridTextColumn Header="TYPE" Binding="{Binding Type}" Width="110"/>

                                <!-- Captain with Icon -->
                                <DataGridTemplateColumn Header="CAPTAIN" Width="*" MinWidth="140">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="👤" FontSize="14" Margin="0 0 6 0" VerticalAlignment="Center"/>
                                                <TextBlock Text="{Binding CaptainName}" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <DataGridTextColumn Header="LAST VOYAGE" Binding="{Binding LastVoyage}" Width="*" MinWidth="170"/>

                                <!-- Status Badge -->
                                <DataGridTemplateColumn Header="STATUS" Width="130">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border CornerRadius="8" Padding="8 4" HorizontalAlignment="Center">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Status}" Value="Docked">
                                                                <Setter Property="Background" Value="#FFF3CD"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Maintenance">
                                                                <Setter Property="Background" Value="#F8D7DA"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="{Binding Status}" 
                                                           FontWeight="SemiBold"
                                                           FontSize="12">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Status}" Value="Docked">
                                                                    <Setter Property="Foreground" Value="#856404"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Maintenance">
                                                                    <Setter Property="Foreground" Value="#721C24"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <!-- Capacity with Icon -->
                                <DataGridTemplateColumn Header="CAPACITY" Width="110">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="📦" FontSize="14" Margin="0 0 6 0" VerticalAlignment="Center"/>
                                                <TextBlock Text="{Binding Capacity}" 
                                                           VerticalAlignment="Center"
                                                           FontWeight="SemiBold"
                                                           Foreground="#0081A7"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <!-- Actions -->
                                <DataGridTemplateColumn Header="ACTIONS" Width="260">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                                <Button Content="Detail"
                                                        Style="{StaticResource PrimaryButton}"
                                                        Width="80"
                                                        Margin="0,0,8,0"
                                                        Command="{Binding DataContext.DetailCommand,
                                                                  RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding ShipId}"/>

                                                <Button Content="Change State"
                                                        Style="{StaticResource SecondaryButton}"
                                                        Width="110"
                                                        Command="{Binding DataContext.ChangeStateCommand,
                                                                  RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding ShipId}"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>

</UserControl>
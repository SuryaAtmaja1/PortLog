<UserControl x:Class="PortLog.Views.DashboardHomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="30">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ================== TOP CARDS ================== -->
        <Grid Grid.Row="0" Margin="0 0 0 20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <!-- CARD 1 -->
            <Border Background="#C9EEFF"
                    CornerRadius="15"
                    Padding="20"
                    Margin="0 0 20 0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">

                    <!-- Icon Placeholder -->
                    <Border Width="60" Height="60" Background="#E0E0E0" CornerRadius="10"/>

                    <StackPanel Margin="15 0">
                        <TextBlock Text="{Binding CurrentSailingShips}"
                                   FontSize="32" FontWeight="Bold"/>
                        <TextBlock Text="Current sailing ships"
                                   FontSize="15" FontStyle="Italic"/>
                    </StackPanel>

                </StackPanel>
            </Border>

            <!-- CARD 2 -->
            <Border Grid.Column="1"
                    Background="#FFF3C4"
                    CornerRadius="15"
                    Padding="20"
                    Margin="0,0,20,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">

                    <Border Width="60" Height="60" Background="#E0E0E0" CornerRadius="10"/>

                    <StackPanel Margin="15 0">
                        <TextBlock Text="{Binding WeeklyRevenueDisplay}"
                                   FontSize="32" FontWeight="Bold"/>
                        <TextBlock Text="This week's revenue"
                                   FontSize="15" FontStyle="Italic"/>
                    </StackPanel>

                </StackPanel>
            </Border>

            <!-- CARD 3 -->
            <Border Grid.Column="2"
                    Background="#C9FFE2"
                    CornerRadius="15"
                    Padding="20">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">

                    <Border Width="60" Height="60" Background="#E0E0E0" CornerRadius="10"/>

                    <StackPanel Margin="15 0">
                        <TextBlock Text="{Binding WeeklyVoyages}"
                                   FontSize="32" FontWeight="Bold"/>
                        <TextBlock Text="This week's voyages"
                                   FontSize="15" FontStyle="Italic"/>
                    </StackPanel>

                </StackPanel>
            </Border>
        </Grid>

        <!-- ================== MAIN GRID ================== -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- SUMMARY -->
            <Border Grid.Row="0"
                    Grid.Column="0"
                    Background="#F5F9F9"
                    CornerRadius="15"
                    Padding="20"
                    Margin="0,0,20,10" Grid.RowSpan="2">
                <TextBlock Text="Summary" FontSize="26" FontWeight="Bold"/>
            </Border>

            <!-- FLEET -->
            <Border Grid.Row="0"
                    Grid.Column="1"
                    Background="#F5F9F9"
                    CornerRadius="15"
                    Padding="20"
                    Margin="0 0 0 20">
                <TextBlock Text="Fleet" FontSize="26" FontWeight="Bold"/>
            </Border>

            <!-- LATEST VOYAGE -->
            <Border Grid.Row="1"
                    Grid.Column="1"
                    Background="#F5F9F9"
                    CornerRadius="15"
                    Padding="20"
                    Margin="0 0 0 10">
                <TextBlock Text="Latest voyage" FontSize="26" FontWeight="Bold"/>
            </Border>

        </Grid>

    </Grid>
</UserControl>
